---
import "@/styles/global.css";
import { ClientRouter } from "astro:transitions";
import "@fontsource/poppins/400.css";
import "@fontsource/poppins/500.css";
import "@fontsource/poppins/700.css";
import NavBar from "@components/shared/NavBar.astro";
import SearchOverlay from "@components/blog/search/SearchOverlay.astro";
import Footer from "@components/shared/Footer.astro";
import { pwaInfo } from "virtual:pwa-info";
import SpeedInsights from "@vercel/speed-insights/astro";
import { getLangFromUrl } from "@i18n/utils";
import { useTranslations } from "@i18n/translations";
import { createLocalizedConstants } from "@lib/constants";
const NODE_ENV = import.meta.env.NODE_ENV;

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const constants = createLocalizedConstants(lang);

interface Props {
  title?: string;
  description?: string;
  image?: string;
  withNavBar?: boolean;
  withFooter?: boolean;
  linksInHead?: string[];
  direction?: "ltr" | "rtl";
}

const {
  title = constants.SITE_CONFIG.title,
  description = constants.SITE_CONFIG.description,
  image,
  withNavBar = true,
  withFooter = true,
  linksInHead = [],
  direction = constants.LANGUAGE_DIRECTION,
} = Astro.props;
---

<!doctype html>
<html lang={direction === "rtl" ? "ar" : "en"} dir={direction}>
  <head>
    <!-- Critical theme initialization -->
    <script is:inline>
      (function () {
        const html = document.documentElement;
        html.classList.remove("light", "dark");

        try {
          const themePref = localStorage.getItem("theme");
          const effectiveTheme =
            themePref === "light"
              ? "light"
              : themePref === "dark"
                ? "dark"
                : window.matchMedia("(prefers-color-scheme: dark)").matches
                  ? "dark"
                  : "light";

          html.classList.add(effectiveTheme);

          // Set data attribute for system preference
          if (!themePref) {
            html.dataset.themeOrigin = "system";
          }
        } catch (e) {
          html.classList.add("dark");
        }
      })();
    </script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="keywords"
      content={constants.SITE_CONFIG.keywords}
    />

    <title>{title}</title>

    <meta name="description" content={description} />

    <!-- IndexNow API Key -->
    <meta name="indexnow" content="343b4cd9b94d455c965aa48b55159df8" />

    <link rel="sitemap" href="/sitemap-index.xml" />

    <!-- Canonical URL -->
    <link rel="canonical" href={`https://saifabdelrazek.com${Astro.url.pathname}`} />

    <!-- Add hreflang links for SEO -->
    <link rel="alternate" hreflang="en" href={`https://saifabdelrazek.com${Astro.url.pathname.replace(/^\/ar/, '')}`} />
    <link rel="alternate" hreflang="ar" href={`https://saifabdelrazek.com/ar${Astro.url.pathname.replace(/^\/ar/, '')}`} />
    <link rel="alternate" hreflang="x-default" href={`https://saifabdelrazek.com${Astro.url.pathname.replace(/^\/ar/, '')}`} />

    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content={constants.SITE_CONFIG.name} />

   <ClientRouter  />
    {pwaInfo && <Fragment set:html={pwaInfo.webManifest.linkTag} />}

    <script>
      // Listen for navigation events
      document.addEventListener("astro:after-swap", () => {
        const currentTheme =
          localStorage.getItem("theme") ||
          (window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light");
        document.documentElement.className = currentTheme;
      });
    </script>

    {NODE_ENV === "production" && <script
      is:inline
      defer
      src="https://insights.a8k.dev/script.js"
      data-website-id="40f66a27-85f1-41cf-84da-8b3ad7d73f80"></script>}
    {linksInHead.map((link) => <Fragment set:html={link} />)
  }
    
    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Saif Abdelrazek",
        "alternateName": "A8K",
        "url": "https://saifabdelrazek.com",
        "jobTitle": "Full-stack Developer",
        "description": "Full Stack Developer & High School Student passionate about web development and open source.",
        "brand": {
          "@type": "Brand",
          "name": "A8K",
          "description": "A numeronym-style developer signature derived from the surname Abdelrazek (A + 8 removed letters + K).",
          "url": "https://a8k.dev",
          "sameAs": "https://saifabdelrazek.com/a8k"
        },
        "sameAs": [
          "https://github.com/saif-abdelrazek",
          "https://linkedin.com/in/saifabdelrazek",
          "https://a8k.dev"
        ],
        "knowsAbout": ["Web Development", "JavaScript", "TypeScript", "React", "Node.js", "Astro"],
        "alumniOf": {
          "@type": "EducationalOrganization",
          "name": "Gharbiya STEM High School"
        }
      }
    </script>
  </head>
  <body>
    {NODE_ENV === "production" && <SpeedInsights />}
    {withNavBar && <NavBar />}
    <SearchOverlay />
    <main>
      <slot />
    </main>
    {withFooter && <Footer />}
    <script src="/src/pwa.ts"></script>
  </body>

</html>
