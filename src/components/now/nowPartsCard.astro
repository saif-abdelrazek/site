---
interface Props {
  title: string;
  icon?: string;
  class?: string;
}

const { title, icon, class: className } = Astro.props;
---

<div
  class={[
    "flex flex-col justify-between rounded-2xl border border-blue-100 light:bg-blue-50 p-4 shadow-md backdrop-blur-md transition-all duration-250 ease-out hover:shadow-lg hover:border-blue-300 light:hover:bg-blue-50/80",
    "dark:border-sky-800 dark:bg-gradient-to-br dark:from-slate-900/80 dark:via-slate-800/80 dark:to-slate-900/60 dark:shadow-md dark:backdrop-blur-md dark:hover:border-sky-700",
    className,
  ].join(" ")}
>
  <div
    class="mb-2 flex items-center gap-2 border-b border-blue-100/40 pb-1 dark:border-sky-900/40"
  >
    {icon && <span class={icon + " text-xl text-blue-400 dark:text-sky-400"} />}
    <h3 class="text-lg font-semibold text-blue-700 dark:text-sky-200">
      {title}
    </h3>
  </div>
  <div class="text-base text-blue-700 dark:text-sky-100">
    <slot />
  </div>
</div>
